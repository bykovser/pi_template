# 📁 Clerk Template — Скелет для твоего агента

Этот скелет скопирован с живого Clerk (Жанночка), но **вычищен от личных данных**.

## Что внутри

```
.pi_template/
├── agent/
│   ├── agents/               # Суб-агенты (можно свои)
│   ├── data/                 # Твои данные, память, профиль
│   │   ├── profile.yaml      ← ГЛАВНЫЙ ФАЙЛ: личность агента
│   │   ├── diary.md           # Дневник агента
│   │   ├── tasks.json         # Задачи
│   │   ├── notes/             # Заметки (память, инcтрукции)
│   │   │   ├── primary/       # Загружаются при старте
│   │   │   └── secondary/     # По запросу
│   │   └── memory.db          # Долговременная память (SQLite)
│   ├── auth.json              # 🔑 API ключи (впиши свои!)
│   ├── settings.json          # Настройки pi
│   └── extensions/            # Расширения (Clerk, web-read и т.д.)
├── extensions/web-read/       # Расширение чтения веб-страниц
└── CLAUDE.md                  # Контекст для Claude Code
```

## 🚀 Быстрый старт

### 1. Впиши API ключи
Открой `agent/auth.json` и вставь свои ключи:
```json
{
  "openrouter": {
    "type": "api_key",
    "key": "sk-or-v1-ТВОЙ_КЛЮЧ"
  }
}
```

### 2. Настрой персонажа
Открой `agent/extensions/clerk/data/profile.yaml`:
- **personality** — имя, характер, тон (оставь пример или перепиши)
- **rules** — правила поведения (оставлены как пример)
- **telegram** — 👇 см. пункт 4
- **facts** — факты о тебе (можно оставить пустым `[]`)

### 3. Активируй Telegram (опционально)
В `profile.yaml` в разделе `telegram` укажи:
```yaml
telegram:
  botToken: "YOUR_BOT_TOKEN"    # от @BotFather
  chatId: 123456789             # твой chat_id
```

### 4. Запусти
```bash
pi
```

---

## 🧹 Что вычищено

- API ключи (OpenRouter) — заменены на заглушку
- TG токен и chat_id — удалены
- Все личные воспоминания и факты
- Дневник агента — оставлен как пример
- Заметки — вычищены, оставлена структура
- Долговременная память (memory.db) — удалена
- Сессии — очищены

## 🎭 Характер оставлен как пример

В `profile.yaml` и `instructions.md` оставлен **характер Жанночки** как референс:
- Прямая, матерная, рубленая, заботливая, чёрный юмор
- Правила: не писать после 23:00, отвечать кратко, предлагать автоматизацию

Можешь переписать под своего персонажа или оставить как есть и донастроить.

---

*Сделано с ❄️ Жанночкой*
